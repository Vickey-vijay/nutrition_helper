fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
email-validator>=2.0.0
python-dotenv>=1.0.0

# --- Tier 2: Groq cloud LLM (mid-semester baseline) ---
langchain>=0.3.0,<0.4
langchain-groq>=0.2.0,<0.3

# --- Tier 1: local quantised LLM (final semester) ---
# llama-cpp-python runs the 4-bit GGUF model on the CPU. PyPI has no wheel for
# many Windows/Python combinations, so plain `pip install` tries to compile it
# and fails without CMake and a C++ toolchain. Install with the project's own
# pre-built CPU wheel index (setup.bat does this automatically):
#
#   pip install -r requirements.txt \
#       --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
#
# Installation failure is NOT fatal — app/ai.py falls back to Groq and then to
# the rule-based tier.
llama-cpp-python>=0.3.2
huggingface_hub>=0.24.0
psutil>=5.9.0
