fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
email-validator>=2.0.0
python-dotenv>=1.0.0

# --- Tier 2: Groq cloud LLM (mid-semester baseline) ---
langchain>=0.3.0,<0.4
langchain-groq>=0.2.0,<0.3

# --- Tier 1: local quantised LLM (final semester) ---
# llama-cpp-python runs the 4-bit GGUF model on the CPU. Pre-built CPU wheels
# exist for Windows/macOS/Linux on recent Python versions; if pip has to build
# from source it needs CMake and a C++ compiler. Installation failure is NOT
# fatal — app/ai.py falls back to Groq and then to the rule-based tier.
llama-cpp-python>=0.3.2
huggingface_hub>=0.24.0
psutil>=5.9.0
